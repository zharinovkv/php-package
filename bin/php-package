#!/usr/bin/env php

<?php

if (is_file(__DIR__.'/../autoload.php') === true) {
    include_once __DIR__.'/../autoload.php';
} else {
    include_once 'PHP/CodeSniffer/autoload.php';
}

echo 'what is your name?: ';
$name = trim(fgets(STDIN));

$user = new PhpPackage\User($name);

var_dump($user);

